import styled from "styled-components";
import siteTheme from "../../styles/Theme.styled";

const Title = styled.div `
  margin: 0 0 19px 0;
  letter-spacing: 0.3px;
  font-size: 16px;
  font-weight: 700;
  color: ${siteTheme.colors.title};
`

export default Title;